import { Link } from "react-router-dom"

const Brand = () =>{
    return (<Link to={`/`} style={{textDecoration:'none', textDecorationColor:'none'}}>RaimondiStore
    </Link>
    )
}

export default Brand
